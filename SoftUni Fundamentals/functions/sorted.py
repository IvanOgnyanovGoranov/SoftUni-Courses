def my_function(elements):
    my_list = []
    for i in integers:
        my_list.append(int(i))
    numbers_sorted = sorted(my_list)
    return numbers_sorted
integers = input().split(" ")
print(my_function(integers))
